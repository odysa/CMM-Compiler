package ast;

import parser.Token;
import java.util.Iterator;

public class CToken implements Iterable<CToken> {
    private Token token;
    @Override
    public Iterator<CToken> iterator() {
        return null;
    }
}
